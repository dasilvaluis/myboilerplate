Front End Gulp Boilerplate
==========================

Front-end boilerplate for Gulp with everything you need to get started.

* Download/Clone repo
* `npm install`
* `npm install -g gulp`
* `bower install`

In development mode:

* `gulp`

For deploynment:

* `gulp deploy`